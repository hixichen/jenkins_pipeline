pipeline {

    agent any

    stages {
        stage('Validate ENV') {
        
            steps {
                        echo 'PASS ENV validation'
            }
        }

    } // end of stages
} // end of pipeline